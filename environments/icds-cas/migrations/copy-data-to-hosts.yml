copy_files:
  - pgshard1:
      - source_host: <source>
        source_dir: <source_root>/pgshard1/postgres/9.6/
        target_dir: /opt/data/postgresql/9.6/
        files:
          - data/
  - pgshard1standby:
      - source_host: <source>
        source_dir: <source_root>/pgshard1/postgres/9.6/
        target_dir: /opt/data/postgresql/9.6/
        files:
          - data/

  # other shards

  - pgucr:
      - source_host: <source>
        source_dir: <source_root>/pgucr/postgres/9.6/
        target_dir: /opt/data/postgresql/9.6/
        files:
          - data/
  - pgucrstandby1:
      - source_host: <source>
        source_dir: <source_root>/pgucr/postgres/9.6/
        target_dir: /opt/data/postgresql/9.6/
        files:
          - data/
  - pgucrstandby2:
      - source_host: <source>
        source_dir: <source_root>/pgucr/postgres/9.6/
        target_dir: /opt/data/postgresql/9.6/
        files:
          - data/
  # more ucr standbys

  - pgmain:
      - source_host: <source>
        source_dir: <source_root>/pgmain/postgres/9.6/
        target_dir: /opt/data/postgresql/9.6/
        files:
          - data/
  - pgmainstandby:
      - source_host: <source>
        source_dir: <source_root>/pgmain/postgres/9.6/
        target_dir: /opt/data/postgresql/9.6/
        files:
          - data/

  - pgsynclog:
      - source_host: <source>
        source_dir: <source_root>/pgsynclog/postgres/9.6/
        target_dir: /opt/data/postgresql/9.6/
        files:
          - data/

  # ES
  # RIAK
  # COUCH


