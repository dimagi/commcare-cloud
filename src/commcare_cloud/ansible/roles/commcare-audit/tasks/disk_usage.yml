--- 
- name: Running 'df -h' command
  shell: |
    set -o pipefail
    df -h | tee {{ audit_directory }}/disk_usage.txt
  args:
    executable: /bin/bash
  delegate_to: localhost
  become_user: "{{ control_user }}"
