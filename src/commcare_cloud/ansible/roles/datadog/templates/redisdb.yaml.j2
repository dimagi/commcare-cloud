#
# {{ ansible_managed }}
#
init_config:

instances:
  - host: localhost
    port: 6379
{% if redis_password is defined %}
    password: {{ redis_password }}
{% endif %}
    tags:
      - environment:{{ env_monitoring_id }}
