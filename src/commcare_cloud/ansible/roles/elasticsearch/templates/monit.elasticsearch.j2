check file elasticsearch with path {{ elasticsearch_data_dir }}/logs/icds-2.0.log
   group database
   group elasticsearch
   start program = "/bin/systemctl start {{ elasticsearch_service_name }}"
   stop program = "/bin/systemctl stop {{ elasticsearch_service_name }}"
   if content = "java.lang.OutOfMemoryError" then restart
