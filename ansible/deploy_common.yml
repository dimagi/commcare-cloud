- name: Common
  hosts: all
  sudo: yes
  roles:
    - common

- name: Datadog agent
  hosts: all
  sudo: yes
  roles:
    - {role: datadog, tags: datadog, when: DATADOG_ENABLED}

- name: Datadog cloudnat
  hosts:
    - postgresql
  vars_files:
    - roles/datadog/defaults/main.yml
  tasks:
    - include: roles/datadog/tasks/cloudant_integration.yml
      when: DATADOG_ENABLED
      run_once: true
  tags:
    - datadog
    - datadog-cloudant
