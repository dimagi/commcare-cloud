#
# {{ ansible_managed }}
#
{
    "directory": "bower_components",
    "proxy": "http://{{ http_proxy_address }}:{{http_proxy_port}}",
    "https-proxy": "http://{{ http_proxy_address }}:{{http_proxy_port}}"
}
