ALLOWED_HOSTS = {{ groups.captain | unique | to_json }}
RQ_LOG_FILE = '{{ captain_log_dir }}/rq.log'

DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.sqlite3',
        'NAME': '{{ captain_database }}',
    }
}
