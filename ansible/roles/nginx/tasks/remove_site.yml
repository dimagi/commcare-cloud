---
- name: load configuration
  include_vars: "{{ site_name }}.yml"

- name: Remove the site configuration
  sudo: yes
  file:
    path: /etc/nginx/sites-enabled/{{ item.server.file_name }}
    state: absent
  with_items: nginx_sites
