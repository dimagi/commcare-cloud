---
# deploy-staging-db.yml
- name: Databases
  hosts: db
  sudo: yes
  vars_files:
    - "vars/{{ deploy_env }}.yml"
  roles:
    - postgresql
    - couchdb
    - bennojoy.memcached
    - bennojoy.redis
    - elasticsearch
    - ecryptfs
    - monitoring
