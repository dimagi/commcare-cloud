---
# deploy-staging-db.yml
- name: Databases
  hosts: db
  sudo: yes
  serial: 1
  vars_files:
    - "vars/{{ deploy_env }}.yml"
  roles:
    - common
    - postgresql
    - couchdb
    - bennojoy.memcached
    - bennojoy.redis
    - elasticsearch
    - ecryptfs
    - monitoring
